# nano-banana — GEMINI_API_KEY resolves from env first, then macOS Keychain.
# Preferred: store in Keychain, leave this unset.
#   security add-generic-password -s nano-banana -a gemini_api_key -w
GEMINI_API_KEY=AIzaxxxxxxxx
